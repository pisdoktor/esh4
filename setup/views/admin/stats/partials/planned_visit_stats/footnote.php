        <p class="small text-muted mt-4 mb-0">
            Dönem filtresi <strong>planlanan tarih</strong> (<code>planlanantarih</code>) üzerinden uygulanır; yalnızca aktif hastalar (<code>pasif = 0</code>) dahildir.
            Tamamlanan: <code>durum = 1</code>; bekleyen: <code>durum = 0</code>; gecikmiş: bekleyen ve plan tarihi bugünden önce.
        </p>